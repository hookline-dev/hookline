# Онбординг: поднять Hookline локально за 30 минут

> ## ⚠️ ПРОЧТИ ПЕРВЫМ ДЕЛОМ: текущий статус проекта
>
> Отметь галочками, что уже сделано в проекте (обновляет тимлид по мере готовности):
>
> - [x] `.env.example` — файл с настройками
> - [x] `Makefile` — короткие команды `make up`, `make test`
> - [x] `docker-compose.yml` — рецепт для базы данных
> - [x] Миграции — SQL, создающий таблицы (задача «Миграция 0001»)
> - [ ] `make demo` и `make seed` — демо-сценарий и тестовые данные (задача «Миграция 0001»)
> - [ ] `make demo` и `make seed` — демо-сценарий и тестовые данные
>
> **Команды ниже работают только для тех пунктов, где стоит галочка.** Это не поломка и не твоя ошибка: инфраструктура пишется в фазе 0 (задачи «docker-compose и Makefile», «Миграция 0001» на доске).
>
> **Что доступно прямо сейчас, пока галочек нет:**
>
> ```bash
> git clone git@github.com:hookline-dev/hookline.git
> cd hookline
> go build ./...            # проверить, что код собирается
> go run ./cmd/hookline     # запустить то, что есть
> go test ./...             # прогнать тесты
> ```
>
> Этого достаточно, чтобы взять и сделать задачу из «чистых» модулей (`signing`, `backoff`, `matcher`, `sink`) — им база и docker не нужны вообще. Что брать — смотри `docs/NEWCOMER_PATH.md`, этап 1.

Цель этого файла — чтобы ты с нуля запустил проект у себя на компьютере и дошёл до первой задачи. Если на любом шаге застрял дольше 15 минут — это не твоя вина, а пробел в этой инструкции: напиши в `#blockers`, мы поможем и допишем сюда решение (см. раздел «Частые проблемы»).

Перед этим файлом должен быть пройден `docs/GIT_FOR_BEGINNERS.md` (аккаунт, git, SSH-ключ, клонирование репозитория).

---

## Что нужно установить

| Инструмент | Зачем | Где взять / как проверить |
|---|---|---|
| **Go 1.26+** | язык, на котором пишем | go.dev/dl → проверка: `go version` |
| **Docker + Docker Compose** | поднимает базу и сервисы одной командой, без ручной установки Postgres | docker.com/products/docker-desktop → проверка: `docker --version` и `docker compose version` |
| **make** | запускает готовые команды проекта короткими словами | Linux/macOS обычно уже есть; Windows — идёт с Git Bash или через WSL. Проверка: `make --version` |
| **git** | работа с кодом | уже установлен на прошлом шаге |

> Docker Desktop должен быть **запущен** (иконка кита в трее/меню). Если он выключен, любая команда `docker ...` и `make up` выдадут ошибку «cannot connect to the Docker daemon».

---

## Шаги запуска

Все команды выполняются внутри папки проекта (`cd hookline`).

**1. Скопируй файл настроек.**
```bash
cp .env.example .env
```
`.env.example` — образец с настройками (адрес базы, порты и т.п.). Команда делает из него твой личный `.env`. Его править не нужно — значений по умолчанию хватает. Коммитить `.env` нельзя (он уже в `.gitignore`).

**2. Подними инфраструктуру.**
```bash
make up
```
Эта команда через Docker поднимет базу данных PostgreSQL, сервис `hookline`, воркеры и тестовый приёмник `sink`. Первый раз будет качать образы — пара минут, это нормально. Готово, когда в выводе перестанут бежать строки и вернётся приглашение терминала.

**3. Создай таблицы в базе (миграции).**
```bash
make migrate
```
«Миграция» — это применение SQL-скриптов, которые создают нужные таблицы. Без этого база пустая и приложение работать не будет.

**4. Убедись, что тесты проходят.**
```bash
make test
```
Все тесты должны быть зелёными. Это же ты будешь запускать перед каждым PR.

**5. Проверь линтер.**
```bash
make lint
```
Первый запуск сам скачает линтер в папку `./bin` — это нормально, займёт полминуты.

Если все пять шагов прошли — поздравляю, окружение готово. 🎉

> **Появится позже** (задачи ещё в работе): `make demo` — прогон демо-сценария, и дашборд на http://localhost:8080.

---

## Полезные команды на каждый день

```bash
make up            # поднять базу в docker
make down          # погасить контейнеры (данные сохранятся)
make down-v        # погасить И удалить данные базы (полный сброс)
make logs          # смотреть логи контейнеров (Ctrl+C — выйти)
make ps            # какие контейнеры запущены
make psql          # открыть консоль psql внутри контейнера с базой
make run           # запустить приложение локально (без docker)
make test          # тесты с детектором гонок
make test-short    # быстрые тесты без -race
make fmt           # отформатировать код
make vet           # встроенный анализатор ошибок
make lint          # проверка стиля кода (то же, что проверяет CI)
make check         # всё сразу: fmt + vet + lint + test
make migrate       # применить миграции
make help          # показать все доступные команды с описаниями
```

Не помнишь команду — просто набери `make help`.

---

## Частые проблемы

Этот раздел наполняется по мере того, как каждый проходит онбординг (задача «Частые проблемы онбординга», см. `docs/NEWCOMER_PATH.md` §1.2). Столкнулся с проблемой, которой тут нет, — реши её и допиши сюда PR-ом. Так ты и себе поможешь, и следующему новичку.

**`make: command not found` (обычно Windows).**
make не установлен или ты не в Git Bash. Открой Git Bash (не cmd/PowerShell) или поставь make через пакетный менеджер / используй WSL.

**`docker: cannot connect to the Docker daemon`.**
Docker Desktop не запущен. Запусти его, дождись, пока иконка перестанет мигать, повтори команду.

**`port is already allocated` / порт занят.**
Какая-то программа уже слушает нужный порт (часто — ранее не погашенный запуск). Сделай `make down`, при необходимости перезапусти Docker Desktop, потом `make up`.

**`make migrate` падает с ошибкой подключения к базе.**
База ещё не успела подняться после `make up`. Подожди 10–15 секунд и повтори. Если не помогло — `make down && make up`, дай базе стартовать, потом миграции.

**Порт 8080 открывается, но страница пустая / ошибка.**
Скорее всего, пропущен `make migrate` или `make demo`. Пройди шаги 3–4 заново.

<!-- Добавляй сюда свои случаи: -->
<!-- **Симптом.** Причина и решение. -->

---

## Если инфраструктура ещё не готова — как всё равно работать

Модули `signing`, `backoff`, `matcher` и `cmd/sink` — это **чистые функции**: обычный Go-код без базы, сети и docker. Их можно писать и тестировать хоть в первый день:

```bash
go test ./internal/backoff/          # тесты одного пакета
go test -race ./...                  # все тесты с детектором гонок
go vet ./...                         # встроенный анализатор ошибок
gofmt -l .                           # какие файлы не отформатированы
```

Именно поэтому задачи новичков в роадмапе стоят в «дорожке чистых модулей» — они ничего не ждут.

## Что дальше

1. Открой **`docs/NEWCOMER_PATH.md`** — там все задачи по этапам и объяснение, за что браться первым. Это главный документ новичка.
2. Прочитай `CONTRIBUTING.md` (правила разработки) и `docs/GITHUB_PROJECTS_GUIDE.md` (как устроена доска и ритуалы), если ещё не.
3. Зайди на доску проекта, колонка **Ready**, найди задачу с меткой **`good-first-issue`**.
4. Назначь её на себя, переведи в **In Progress** и — по циклу из `docs/GIT_FOR_BEGINNERS.md` — доведи до первого PR.
5. Пошаговые инструкции по конкретным задачам — в `docs/TASK_GUIDES.md`.

Цель первой недели простая: один смерженный PR, любого размера. Даже правка опечатки считается — важно один раз пройти весь путь.
