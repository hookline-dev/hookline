global: {scrape_interval: 5s}
scrape_configs:
  - job_name: hookline
    static_configs: [{targets: [hookline-api:8080]}]
  - job_name: hookline-workers
    dns_sd_configs:
      - names: [hookline-worker]
        type: A
        port: 9093
